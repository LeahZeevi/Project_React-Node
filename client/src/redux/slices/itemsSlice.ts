import { createSlice } from "@reduxjs/toolkit"

const initialState={

}

const itemSlice=createSlice({
    name:"items",
    initialState,
    reducers:{

    }
    
})
export default itemSlice.reducer